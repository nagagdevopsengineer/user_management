stages:
    - build
    - deploy

build:
    stage: build
    image: maven:latest
    script:
        - echo "Building app..."
        - ./mvnw clean install
        - echo "Finished building the app."
    artifacts:
        #expire_in: 1 week
        paths:
           - JAVA_HOME = "%JAVA_HOME%" >&2
    only:
        - user-dev
