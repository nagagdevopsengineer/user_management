apiVersion: v1
kind: Service
metadata:
  name: usermanagement
  namespace: usermanagement
  labels:
    app: usermanagement
spec:
  type: NodePort
  ports:
  - name: https
    port: 80
    targetPort: 8082
  selector:
    app: usermanagement